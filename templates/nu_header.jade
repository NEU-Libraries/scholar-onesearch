if target.name === "Development"
  // Using no CSS in the view code so that we can show live changes through our local server, use the Primo BackOffice to upload testing and production CSS and JS.
  link(rel='stylesheet', href= target.assetPath + '/css/legacy.css')
  link(rel='stylesheet', href= target.assetPath + '/css/style.css')

//nu_header start

//if IE 7
  link(rel='stylesheet', href='//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome-ie7.min.css')

//if lte IE 9
  link(rel='stylesheet', href=target.assetPath + '/css/style-blessed.css')

//if lt IE 9
  script(src='//oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js')
  script(src='//oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js')

script(src= target.assetPath + '/js/modernizr.min.js')


// From HTML5BP plugins.js @link https://github.com/h5bp/html5-boilerplate/blob/master/js/plugins.js#L1
script.
  // Avoid `console` errors in browsers that lack a console.
  (function() {
  var method;
  var noop = function () {};
  var methods = [
  'assert', 'clear', 'count', 'debug', 'dir', 'dirxml', 'error',
  'exception', 'group', 'groupCollapsed', 'groupEnd', 'info', 'log',
  'markTimeline', 'profile', 'profileEnd', 'table', 'time', 'timeEnd',
  'timeStamp', 'trace', 'warn'
  ];
  var length = methods.length;
  var console = (window.console = window.console || {});
  while (length--) {
  method = methods[length];
  // Only stub undefined methods.
  if (!console[method]) {
  console[method] = noop;
  }
  }
  }());

script.
  (function() {
    var styleSheetList = document.styleSheets;
    for (var i = 0; i < styleSheetList.length; i++){
      var href = styleSheetList[i].href;
      if (href){
        href = href.toString();
        if(href.search('my_shelf.css') > 0 ){
          document.body.className = (document.body.className + ' sos-eshelf');
          return;
        }
      }

      }
    }());

script.
  if (navigator.userAgent.match(/IEMobile\/10\.0/)) {
    var msViewportStyle = document.createElement("style")
    msViewportStyle.appendChild(
      document.createTextNode(
        "@-ms-viewport{width:auto!important}"
      )
    )
    document.getElementsByTagName("head")[0].appendChild(msViewportStyle)
  }
  var worldcatLogo =  "#{target.assetPath}/images/worldcat-logo.png";



unless target.name === "Production"
  h1= target.name
a.nu-lib-logo(href='http://library.northeastern.edu', title='Notheastern Univerity, University Libraries Homepage')
  span.text-hide Northeastern University, University Libraries

a.logo.scholar-one-search(href='search.do', title='New Search')
  span.text-hide Scholar OneSearch
//nu_header end

noscript
  .alert.alert-danger
     strong
      | Warning!
     | Javascript must be enabled to properly use this website, please enable javascript.
